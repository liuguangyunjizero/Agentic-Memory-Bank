# ===========================================
# Agentic Memory Bank - 环境变量配置模板
# ===========================================
# 复制此文件为 .env 并填入您的实际配置

# ===== LLM 配置 =====
# LLM提供商：deepseek | openai | local
LLM_PROVIDER=deepseek

# DeepSeek 配置（如果使用 deepseek）
DEEPSEEK_API_KEY=your-deepseek-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# OpenAI 配置（如果使用 openai）
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# 本地模型配置（如果使用 local）
LOCAL_BASE_URL=http://127.0.0.1:6001/v1
LOCAL_MODEL=default

# LLM 通用参数
LLM_TEMPERATURE=0.6
LLM_MAX_TOKENS=4096
LLM_TOP_P=0.95

# ===== Embedding 配置 =====
# 本地 SentenceTransformer 模型
# 可选：all-MiniLM-L6-v2（快速） | all-mpnet-base-v2（精确）
EMBEDDING_MODEL=all-MiniLM-L6-v2

# ===== 检索配置 =====
# Top-K 检索数量
RETRIEVAL_K=5
# 混合检索权重（BM25的权重，embedding权重为1-alpha）
RETRIEVAL_ALPHA=0.5

# ===== ReAct 配置 =====
# 每次任务的最大LLM调用次数
MAX_LLM_CALL_PER_RUN=60
# 最大上下文token数
MAX_CONTEXT_TOKENS=32000

# ===== 路径配置 =====
# 临时文件目录（多模态暂存）
TEMP_DIR=data/temp
# 正式存储目录
STORAGE_DIR=data/storage

# ===== Agent 窗口配置 =====
# 各Agent的窗口大小（token）
CLASSIFICATION_AGENT_WINDOW=8000
STRUCTURE_AGENT_WINDOW=8000
ANALYSIS_AGENT_WINDOW=8000
INTEGRATION_AGENT_WINDOW=8000
PLANNING_AGENT_WINDOW=8000

# ===== Agent LLM 参数配置 =====
# 每个Agent的专用LLM参数（temperature控制随机性，top_p控制采样范围）
# 建议值：
# - 需要精确性的Agent（Structure, Integration）：temperature=0.1-0.2, top_p=0.8-0.85
# - 需要创造性的Agent（Planning）：temperature=0.5-0.7, top_p=0.9-0.95
# - 平衡型Agent（Classification, Analysis）：temperature=0.3-0.5, top_p=0.85-0.9

# Classification Agent（上下文分类）
CLASSIFICATION_AGENT_TEMPERATURE=0.4
CLASSIFICATION_AGENT_TOP_P=0.9

# Structure Agent（结构化压缩 - 需要精确保留数据）
STRUCTURE_AGENT_TEMPERATURE=0.1
STRUCTURE_AGENT_TOP_P=0.8

# Analysis Agent（关系分析）
ANALYSIS_AGENT_TEMPERATURE=0.4
ANALYSIS_AGENT_TOP_P=0.9

# Integration Agent（冲突整合 - 需要精确性）
INTEGRATION_AGENT_TEMPERATURE=0.2
INTEGRATION_AGENT_TOP_P=0.85

# Planning Agent（任务规划 - 需要一定创造性）
PLANNING_AGENT_TEMPERATURE=0.6
PLANNING_AGENT_TOP_P=0.95

# ReAct Agent（推理执行）
REACT_AGENT_TEMPERATURE=0.6
REACT_AGENT_TOP_P=0.95

# ===== 超长文本处理 =====
# 分块加载的窗口利用率
CHUNK_RATIO=0.9

# ===== 搜索API配置 =====
# Serper API Key（用于真实网络搜索）
# 注册地址：https://serper.dev/
SERPER_API_KEY=your-serper-api-key-here

# Jina Reader API Key（用于网页内容提取）
# 注册地址：https://jina.ai/reader/
JINA_API_KEY=your-jina-api-key-here

# ===== 日志配置 =====
# 日志级别：DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
